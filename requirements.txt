# Production runtime dependencies only.
# Dev tools (pytest, ruff, mypy) live in pyproject.toml `dev` extra
# — install with `pip install -e ".[dev]"`.

# Telegram Bot
aiogram==3.27.0

# HTTP Client
httpx==0.28.1
tenacity==9.1.4

# Configuration
# pydantic 2.13.x blocked by aiogram 3.27.0 (pydantic<2.13). Keep 2.12.x.
pydantic==2.12.5
pydantic-settings==2.13.1

# Database
aiosqlite==0.22.1

# Logging
structlog==25.5.0
