# Dead-code audit — TG_arr

No independently provable dead production module or unreachable production branch found. Existing compatibility re-exports in `bot/handlers/search/__init__.py` are used by callers/tests.
