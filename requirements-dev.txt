# Development-only dependencies.
# Install with: pip install -r requirements.txt -r requirements-dev.txt
#
# DEP-04: these exact pins must stay inside the version ranges declared in
# pyproject.toml `[project.optional-dependencies].dev`. `pip install -e ".[dev]"`
# and this file resolve to the same toolchain.

pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
ruff==0.15.20
mypy==1.18.2
# DEP-07/DEAD-16: needed by the compose-config guard tests in
# tests/test_r4_C1-deploy-docs.py (yaml.safe_load); without it those tests
# silently skip instead of validating docker-compose.yml/.dev.yml.
# Lowercase to match the exact-string comparison in the pyproject [dev] guard
# test below (pip itself is case-insensitive, but that test isn't).
pyyaml==6.0.3
