# Production runtime dependencies only.
# Dev tools (pytest, ruff, mypy) live in pyproject.toml `dev` extra
# — install with `pip install -e ".[dev]"`.

# Telegram Bot
aiogram==3.29.1

# HTTP Client
httpx==0.28.1
tenacity==9.1.4
# DEP-01: aiohttp powers the inbound webhook server (bot/webhook.py, feature
# #8). It arrives transitively via aiogram (aiogram 3.29.1 requires
# aiohttp<3.15,>=3.9.0), but that leaves the version of a network-facing HTTP
# server undeclared and unreproducible. Pin it explicitly.
aiohttp==3.13.4

# Configuration
# DEP-05: aiogram 3.29.1 relaxed the cap to pydantic<2.14, so 2.13.x is now
# allowed (aiogram 3.27 had required pydantic<2.13).
pydantic==2.13.4
pydantic-settings==2.14.2

# Database
aiosqlite==0.22.1

# Logging
structlog==26.1.0
