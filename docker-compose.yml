# Pin the compose project name so the deploy keeps using the existing
# `tgarr_bot-data` volume. Without this, Compose v2 derives the project from
# the directory (`TG_arr` -> `tg_arr`) and would create a fresh EMPTY
# `tg_arr_bot-data` volume, orphaning the live database (allowlist, prefs).
name: tgarr

services:
  tg-arr-bot:
    build:
      context: .
      dockerfile: Dockerfile
    # DEPLOY-02: explicit name so `make deploy`/`make rollback` can tag a
    # known image reference regardless of the compose project directory name
    # (which would otherwise drive the default `<dir>-tg-arr-bot` tag).
    image: tg-arr-bot:latest
    container_name: tg-arr-bot
    restart: unless-stopped
    environment:
      # Required
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:?Set TELEGRAM_BOT_TOKEN}
      # Rolled back to *arr on 2026-08-10. Radarr and Sonarr own the catalog
      # and the quality profiles; Prowlarr serves free-text search for titles
      # not yet in the catalog, so the bot needs its own credentials again.
      RADARR_URL: ${RADARR_URL:?Set RADARR_URL}
      RADARR_API_KEY: ${RADARR_API_KEY:?Set RADARR_API_KEY}
      SONARR_URL: ${SONARR_URL:?Set SONARR_URL}
      SONARR_API_KEY: ${SONARR_API_KEY:?Set SONARR_API_KEY}
      PROWLARR_URL: ${PROWLARR_URL:?Set PROWLARR_URL}
      PROWLARR_API_KEY: ${PROWLARR_API_KEY:?Set PROWLARR_API_KEY}
      # Users
      ALLOWED_TG_IDS: ${ALLOWED_TG_IDS:-}
      ADMIN_TG_IDS: ${ADMIN_TG_IDS:-}
      # qBittorrent (optional)
      QBITTORRENT_URL: ${QBITTORRENT_URL:-}
      QBITTORRENT_USERNAME: ${QBITTORRENT_USERNAME:-admin}
      QBITTORRENT_PASSWORD: ${QBITTORRENT_PASSWORD:-}
      QBITTORRENT_TIMEOUT: ${QBITTORRENT_TIMEOUT:-30.0}
      # Emby (optional)
      EMBY_URL: ${EMBY_URL:-}
      EMBY_API_KEY: ${EMBY_API_KEY:-}
      EMBY_TIMEOUT: ${EMBY_TIMEOUT:-30.0}
      # TorrServer (optional) — streaming contour: "watch now"
      TORRSERVER_URL: ${TORRSERVER_URL:-}
      TORRSERVER_USERNAME: ${TORRSERVER_USERNAME:-admin}
      TORRSERVER_PASSWORD: ${TORRSERVER_PASSWORD:-}
      TORRSERVER_TIMEOUT: ${TORRSERVER_TIMEOUT:-30.0}
      TORRSERVER_SEARCH_TIMEOUT: ${TORRSERVER_SEARCH_TIMEOUT:-60.0}
      TORRSERVER_METADATA_TIMEOUT: ${TORRSERVER_METADATA_TIMEOUT:-30.0}
      EMBY_SYNC_HOOK_URL: ${EMBY_SYNC_HOOK_URL:-}
      EMBY_SYNC_HOOK_TOKEN: ${EMBY_SYNC_HOOK_TOKEN:-}
      EMBY_SYNC_HOOK_TIMEOUT: ${EMBY_SYNC_HOOK_TIMEOUT:-90.0}
      # TMDb (optional)
      TMDB_API_KEY: ${TMDB_API_KEY:-}
      TMDB_LANGUAGE: ${TMDB_LANGUAGE:-ru-RU}
      TMDB_PROXY_URL: ${TMDB_PROXY_URL:-}
      # DEPLOY-02: Lidarr / music (optional) — set these to enable /music & Deezer
      LIDARR_URL: ${LIDARR_URL:-}
      LIDARR_API_KEY: ${LIDARR_API_KEY:-}
      DEEZER_ENABLED: ${DEEZER_ENABLED:-true}
      # slskd / Soulseek (optional) — enables /album and /track
      SLSKD_URL: ${SLSKD_URL:-}
      SLSKD_API_KEY: ${SLSKD_API_KEY:-}
      SLSKD_TIMEOUT: ${SLSKD_TIMEOUT:-30.0}
      SLSKD_SEARCH_TIMEOUT: ${SLSKD_SEARCH_TIMEOUT:-25.0}
      # Navidrome (optional) — read-only "already in the library" check
      NAVIDROME_URL: ${NAVIDROME_URL:-}
      NAVIDROME_USERNAME: ${NAVIDROME_USERNAME:-}
      NAVIDROME_PASSWORD: ${NAVIDROME_PASSWORD:-}
      NAVIDROME_TIMEOUT: ${NAVIDROME_TIMEOUT:-15.0}
      # Notifications
      NOTIFY_DOWNLOAD_COMPLETE: ${NOTIFY_DOWNLOAD_COMPLETE:-true}
      NOTIFY_CHECK_INTERVAL: ${NOTIFY_CHECK_INTERVAL:-60}
      NOTIFY_ADMINS_ON_START: ${NOTIFY_ADMINS_ON_START:-true}
      # Tuning
      TIMEZONE: ${TIMEZONE:-Europe/Moscow}
      # DEPLOY-03: TZ feeds glibc; tzdata is installed in the image.
      TZ: ${TIMEZONE:-Europe/Moscow}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      # OBS-13: rendering format (json for docker logs vs console for a tty)
      # is independent of LOG_LEVEL verbosity.
      LOG_FORMAT: ${LOG_FORMAT:-json}
      AUTO_GRAB_SCORE_THRESHOLD: ${AUTO_GRAB_SCORE_THRESHOLD:-80}
      HTTP_TIMEOUT: ${HTTP_TIMEOUT:-30.0}
      # A Prowlarr search fans out to every configured indexer, so it needs far
      # more headroom than a plain API call (DEPLOY-01: must be listed here, or
      # setting it in .env never reaches the container). 45s, not 25s: RuTracker
      # behind Cloudflare answers 521/522 under load.
      PROWLARR_SEARCH_TIMEOUT: ${PROWLARR_SEARCH_TIMEOUT:-45.0}
      PROWLARR_SEARCH_RETRIES: ${PROWLARR_SEARCH_RETRIES:-1}
      RESULTS_PER_PAGE: ${RESULTS_PER_PAGE:-5}
      # Database path (relative to /app inside the container)
      DATABASE_PATH: ${DATABASE_PATH:-data/bot.db}
    volumes:
      - bot-data:/app/data
    # DEPLOY-05: the container only ever needs to write to /app/data (the
    # named volume above, holding the sqlite db) and /tmp (the liveness file
    # bot/main.py touches for HEALTHCHECK). Everything else — including the
    # app code itself — can be a read-only root filesystem.
    read_only: true
    tmpfs:
      - /tmp
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    stop_grace_period: 30s
    # DEPLOY-04: under Compose V2 (`docker compose`), deploy.resources.limits
    # IS enforced for standalone containers — no Swarm required. (Only the
    # legacy v1 `docker-compose` binary ignored it outside Swarm.) Swarm and
    # Portainer Swarm stacks honour it too.
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.5'

volumes:
  bot-data:
    driver: local
