# Production runtime dependencies only.
# Dev tools (pytest, ruff, mypy) live in pyproject.toml `dev` extra
# — install with `pip install -e ".[dev]"`.

# Telegram Bot
aiogram==3.30.0

# HTTP Client
httpx==0.28.1
tenacity==9.1.4
# DEP-01: aiogram's network transport. The webhook server it was originally
# pinned for (bot/webhook.py) no longer exists and nothing under bot/ imports
# aiohttp (audit 2026-08-13) — the pin stays because aiohttp ships in every
# image anyway (aiogram 3.30.0 requires aiohttp<3.15,>=3.9.0) and an explicit
# version is what keeps a lock regeneration reproducible.
aiohttp==3.14.3

# Configuration
# DEP-05: aiogram 3.30.0 caps pydantic at <2.14, so 2.13.x is the ceiling.
pydantic==2.13.4
pydantic-settings==2.15.0

# Database
aiosqlite==0.22.1

# Logging
structlog==26.1.0
