# Анализ — Качество тестов · TG_arr (раунд 4, 2026-06-30)

Подтверждено находок: **7** (critical=0, high=0, medium=4, low=3). Все прошли состязательную верификацию (CONFIRMED/PLAUSIBLE).

## Средние

### TEST-01: Auth/RateLimit middleware and config access-control (is_user_allowed/is_admin) have zero tests
- **Файл**: `bot/middleware/auth.py:51`
- **Проблема**: The security boundary of the entire bot — AuthMiddleware (rejects users not in ALLOWED_TG_IDS, grants ADMIN role from ADMIN_TG_IDS, auto-creates users, and re-fetches on concurrent-creation conflict at lines 85-90) and RateLimitMiddleware — has no test file at all. config.is_user_allowed / config.is_admin are also never asserted (grep over tests/ for is_user_allowed, is_admin, AuthMiddleware, RateLimit returns nothing but conftest env setup). A regression that parses ALLOWED_TG_IDS wrongly (e.g. off-by-one in CSV split, treating empty string as 'allow all', or admin set leaking) would let any Telegram user issue grab/delete commands and would ship green. The concurrent-creation except branch (lines 85-90) that re-raises when re-fetch also returns None is dead from a test standpoint.
- **Риск**: Unauthorized users could gain access or admins be misidentified with no test to catch it; this is the project's only access-control gate.
- **Решение**: Add tests/test_middleware.py and tests/test_config_auth.py: (1) config.is_user_allowed(id) True for allowed, False for unknown id, and verify behavior on empty/whitespace ALLOWED_TG_IDS; is_admin True only for ADMIN_TG_IDS. (2) AuthMiddleware.__call__ with a mock event from a disallowed user calls event.answer with the rejection and returns None without calling handler; allowed-but-new user triggers db.create_user and sets data['is_admin']; simulate create_user raising IntegrityError then get_user returning the user (conflict path) and assert handler still runs; simulate create_user raising and get_user returning None and assert it re-raises. (3) RateLimitMiddleware blocks the 31st request inside the window and resets after window_seconds.
- **Верификация**: CONFIRMED — The coverage gap is real and reproducible. tests/ (listed via Bash) contains NO test_middleware.py or test_config_auth.py and no file exercising AuthMiddleware/RateLimitMiddleware. Grep over tests/ for `is_user_allowed|is_admin|AuthMiddleware|RateLimit` returns only tests/test_handlers_downloads.py:94 and :113, which pass `is_admin=True` as a kwarg into downloads.handle_delete_torrent/handle_delete_with_files — they neither call nor assert config.is_user_allowed/is_admin. The one "Auth" hit in test_qbittorrent.py is QBittorrentAuthError, unrelated. The cited code exists and matches the descrip
- **Статус**: [x] Исправлено (раунд 4, мультиагент)

### TEST-02: qBittorrent _request() 403 session-expiry re-auth state machine is untested
- **Файл**: `bot/clients/qbittorrent.py:167`
- **Проблема**: test_qbittorrent.py tests login() (200/204/Fails/no-cookie) and add_torrent_url, but _request()'s re-auth-on-403 branch (lines 167-191) is never exercised: on a 403 it clears _authenticated, re-logs in, and reissues the request; if re-auth raises it must NOT retry; timeout/ConnectError on the reissue must map to QBittorrentError. This is exactly the state machine the prompt flags. Scenario: qBit drops the SID cookie mid-session (common after restart) → first request gets 403 → if the re-auth/reissue path has a regression (e.g. infinite loop, swallowed exception, or reissuing without re-login) every torrent action silently fails or hangs. No test pins this behavior.
- **Риск**: A regression in the session-recovery path would make all qBittorrent operations fail after the session expires, with no test coverage.
- **Решение**: Add tests driving _request through a mock httpx client (patch _get_client) where the first request.return_value has status_code=403 and the second returns 200/JSON: assert login() is called exactly once more and the reissued response is returned. Add a case where the second login raises QBittorrentAuthError and assert _request re-raises without a third request. Add a case where the reissued request raises httpx.TimeoutException and assert QBittorrentError is raised.
- **Верификация**: CONFIRMED — Verified against the actual files. bot/clients/qbittorrent.py:147-206 contains _request(), and lines 167-191 implement the 403 session-expiry re-auth state machine exactly as described: on a 403 it sets _authenticated=False, calls _ensure_authenticated() (-> login()); if re-auth raises QBittorrentAuthError/QBittorrentError it re-raises without reissuing (lines 172-173), otherwise it reissues the request once (lines 175-181) and maps TimeoutException/ConnectError on the reissue to QBittorrentError (lines 182-185). This is genuine multi-branch control flow.

Coverage check across the whole suite
- **Статус**: [x] Исправлено (раунд 4, мультиагент)

### TEST-03: NotificationService monitor poller (_monitor_loop/_initial_sync/_check_for_completions/_notify_completion) is untested
- **Файл**: `bot/services/notification_service.py:136`
- **Проблема**: Only force_check, subscribe/unsubscribe and get_stats are tested. The actual completion-notification logic that runs in production — _initial_sync (marks existing torrents notified=True so they don't re-notify), _check_for_completions (detects a tracked torrent transitioning progress<1 → complete and calls _notify_completion exactly once), the removed-then-readded torrent cleanup at lines 170-172, and _notify_completion fan-out to subscribed users with per-user exception isolation — has zero tests. Concrete failure: a torrent that completes during a session must send exactly one notification; a torrent already complete at startup must send none. A regression flipping the notified default (line 162: 'notified': is_complete) would spam users for every pre-existing complete torrent, or silently never notify, and no test would catch it.
- **Риск**: The user-facing notification feature could spam or silently drop completion alerts on regression with nothing to detect it.
- **Решение**: Add async tests using a mock qbittorrent.get_torrents returning controlled sequences: (a) initial_sync then a torrent flips to complete on the next check → send_notification awaited exactly once with that user; (b) torrent complete at first sight (new) → no notification; (c) _notify_completion with two subscribers where the first send raises → second still receives it; (d) a torrent removed from the list is dropped from _tracked_torrents. Drive _check_for_completions directly rather than the sleep loop to avoid timing flakiness.
- **Верификация**: CONFIRMED — Verified directly against current code. tests/test_qbittorrent.py:518 TestNotificationService contains exactly four tests: test_subscribe_user (539), test_unsubscribe_user (544), test_get_stats (550), test_force_check (560). A repo-wide grep for _check_for_completions / _initial_sync / _notify_completion / _monitor_loop in tests/ returns zero matches, and there is no separate tests/test_notification*.py. So the production completion-notification path is genuinely untested. The cited logic exists as described in bot/services/notification_service.py: _initial_sync sets 'notified': True at line 1
- **Статус**: [x] Исправлено (раунд 4, мультиагент)

### TEST-04: Grab flow happy paths (push-approved, direct grab, qBittorrent force_download fallback) are untested
- **Файл**: `bot/services/add_service.py:381`
- **Проблема**: test_add_service.py covers only the SSRF *rejection* branches (push_release.assert_not_called for private URLs) plus one magnet sanity check that stops at push_release. The success-carrying branches of grab_movie_release/grab_series_release/grab_music_release are untested: push approved → returns (True,'Релиз отправлен'); push rejected then direct grab_release succeeds; push+grab fail then force_download routes to qbittorrent.add_torrent_url with the right category ('radarr'/'tv-sonarr'/'music') and the SSRF re-validation at lines 384/536/733; and the rejected-no-fallback path returning the rejection message. This is the core money path of the bot. A regression that, e.g., passes the wrong category, skips the second _validate_download_url, or mis-handles add_torrent_url returning False would ship green. Note add_torrent_url returning False raises QBittorrentError caught by the broad except and returns 'Ошибка загрузки' — no test asserts that mapping either.
- **Риск**: The primary download/grab workflow has no positive-path tests; category mis-routing or fallback breakage would not be caught.
- **Решение**: Add async tests with AsyncMock radarr/sonarr/lidarr+qbittorrent: (1) push_release returns {'approved': True} → (True, success message). (2) push returns {'approved': False,'rejections':[...]} and grab_release succeeds → 'Релиз захвачен'. (3) push rejected, indexer_id=0, force_download=True, qbittorrent.add_torrent_url returns True → assert add_torrent_url called once with category='radarr'/'tv-sonarr'/'music' and a public download_url. (4) same but add_torrent_url returns False → success False and 'Ошибка загрузки через qBittorrent'. (5) rejected with no qbittorrent → message contains the rejection text.
- **Верификация**: CONFIRMED — I opened tests/test_add_service.py, tests/test_lidarr.py, tests/test_qbittorrent.py and bot/services/add_service.py and grepped the whole tests/ tree. The grab-flow tests in test_add_service.py only assert SSRF rejection (radarr/sonarr/lidarr push_release.assert_not_called for private/loopback URLs at lines 204/233/261) plus one magnet sanity test (lines 264-291). _build_add_service has a `qbt` param (line 168/173) but NO test ever passes a non-None qbittorrent; every SSRF release uses indexer_id=0, so the fallback gate `if (release_rejected or force_download) and self.qbittorrent:` at add_ser
- **Статус**: [x] Исправлено (раунд 4, мультиагент)

## Низкие

### TEST-05: add_torrent_url does not handle empty/204 success response (qBit v5.2.0) and no test pins the contract ⚠️PLAUSIBLE
- **Файл**: `bot/clients/qbittorrent.py:422`
- **Проблема**: login() was explicitly hardened for qBittorrent v5.2.0 returning 204/empty body (lines 128-134, with a dedicated test), but add_torrent_url still treats success as strictly result.strip()=='Ok.'. _request() returns None when response.text is empty (lines 194-195). If /torrents/add returns an empty 2xx body (observed with some qBit builds / reverse proxies that strip the 'Ok.' body), result is None → the method returns False and the grab flow reports failure even though the torrent was added (and may even re-add via fallback). The existing tests only feed the literal 'Ok.'/'Fails.' strings, so this realistic edge is unguarded.
- **Риск**: Against qBittorrent v5.2.0+/proxied setups a successful add is reported as failure, triggering misleading user errors or duplicate fallback adds.
- **Решение**: Treat empty/None response (HTTP 2xx) as success in add_torrent_url, e.g. `if result is None or (isinstance(result, str) and result.strip() in ('', 'Ok.')): return True`, keeping the 'Fails.' branch as False. Add a test where _request returns None (empty body) and assert add_torrent_url returns True, and one where it returns 'Fails.' returning False.
- **Верификация**: PLAUSIBLE — The code mechanics are exactly as described and reproducible. In bot/clients/qbittorrent.py, _request() returns None for any 2xx response with an empty body (line 187 `if response.status_code >= 400: raise` is skipped for 2xx; line 194-195 `if not response.text: return None`; the comment at line 193 even states "Many qBittorrent endpoints return empty response"). add_torrent_url() at line 422 checks `if isinstance(result, str) and result.strip() == "Ok."` — a None result is not a str, so it falls into the else branch (lines 425-427), logs an error, and returns False. The caller in bot/services
- **Статус**: [ ] Не исправлено

### TEST-06: detect_with_confidence ambiguous tie-break and music-demotion branches are untested ⚠️PLAUSIBLE
- **Файл**: `bot/services/search_service.py:188`
- **Проблема**: Tests cover series-pattern shortcut, all-lookups-fail, and low-confidence→UNKNOWN, but two decision branches that determine silent content routing are untested: (1) the tie-break at lines 188-190 (top and runner-up within 0.05 and runner_up>0.6 → UNKNOWN so the user is asked) and (2) the music demotion at lines 154-156 (music_score *= 0.7 unless >=0.92) and the query_year→music_score=0.0 rule at line 150-151. Concrete failure: a query matching both a movie and a series with near-equal fuzzy scores must return UNKNOWN (ask user), and a query like an artist name that also weakly matches a movie must not be auto-routed to MUSIC. A regression in the 0.05/0.92 thresholds would auto-pick the wrong type and search the wrong indexer categories — a silent wrong result — with no test to catch it.
- **Риск**: Threshold regressions would silently misclassify queries and route searches to the wrong category, returning wrong results to users undetected.
- **Решение**: Add tests constructing radarr/sonarr fixtures whose titles both fuzzy-match the query closely (e.g. 'The Thing' as both a movie and series with similar names) and assert content_type==UNKNOWN, reason=='ambiguous'. Add a test where only Lidarr returns an artist matching ~0.8 and a movie matches ~0.75, asserting music does NOT win (demotion). Add a test that a year-bearing query with only an artist match returns non-MUSIC because music_score is forced to 0.
- **Верификация**: PLAUSIBLE — I opened bot/services/search_service.py and tests/test_detect_content_type.py (the only test file exercising detect_with_confidence — confirmed via grep across tests/).

The two code branches cited exist and are correctly located:
- Music demotion: lines 150-156. query_year is not None -> music_score=0.0 (150-151); else music_score < 0.92 -> music_score *= 0.7 (154-156).
- Tie-break: lines 188-190. top_score - runner_up_score < 0.05 and runner_up_score > 0.6 -> return UNKNOWN with reason "ambiguous".

Coverage assessment per sub-claim:
1. Tie-break "ambiguous" branch (188-190): NOT specificall
- **Статус**: [ ] Не исправлено

### TEST-07: db.get_session corrupt-session auto-delete-and-return-None path is untested
- **Файл**: `bot/db.py:379`
- **Проблема**: TestCorruptPreferences covers corrupt user-preferences JSON, but the analogous and arguably more important session path (get_session lines 367-389) — where stored session_data fails model_validate, the code logs, calls delete_session, and returns None — has no test. Scenario: a model change makes an old persisted session JSON invalid; on next callback get_session must purge it and return None (so the handler starts fresh) rather than raising. A regression that re-raises instead of deleting would brick every returning user whose stored session predates the change. Also save_search/cleanup_old_searches use explicit BEGIN/rollback with isolation_level=None and have no failure/rollback test.
- **Риск**: A persisted-session schema drift could crash returning users on the corrupt-session path that is currently unverified.
- **Решение**: Add a test that writes invalid JSON (or JSON missing required SearchSession fields) into sessions.session_data for a user, then asserts get_session(user) returns None AND that a subsequent SELECT shows the row was deleted. Optionally add a rollback test for save_search by patching the second execute to raise and asserting no orphan rows remain.
- **Верификация**: CONFIRMED — Verified directly in current code. bot/db.py get_session (lines 357-389) has the described recovery path: on json.loads/model_validate failure it logs (line 380), calls await self.delete_session(user_id) (line 387) and returns None (line 388). The finding's cited line range (367-389) is slightly off — the method starts at 357 — but the error-handling block it emphasizes (379-389) is exactly correct. In tests/test_db.py the ONLY corruption test is TestCorruptPreferences (line 296), which exercises corrupt USER-PREFERENCES JSON via get_user, not the session path. A grep across tests/ for corrupt
- **Статус**: [x] Исправлено (раунд 4, мультиагент)
