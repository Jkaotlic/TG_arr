# Анализ мёртвого кода TG_arr (раунд 5)

Метод: полное чтение всех 40 файлов `bot/` и 30 файлов `tests/`; для каждого подозреваемого символа — grep по проекту (продакшн + тесты + строковые callback_data); `ruff check --select F401,F841,F811,F821` → **0 неиспользуемых импортов/переменных**. Callback-хэндлеры считались «достижимыми», только если существует клавиатура, генерирующая соответствующий `callback_data`.

## Критические

Нет.

## Средние

### DEAD-01: 8 застрявших git-worktrees от старого workflow
- **Файл**: `.claude/worktrees/wf_be70a6e1-f20-{1..8}` (ветки `worktree-wf_be70a6e1-f20-*`)
- **Проблема**: 8 worktrees зависли на коммите `04a38ca` с грязными незакоммиченными изменениями; master ушёл на 10 коммитов вперёд. Это 8 полных копий репозитория — мусор в `git worktree list`, `git branch`, ловушка для grep/IDE-индексации.
- **Риск**: Низкий (не влияет на рантайм)
- **Решение**: заархивировать диффы в `analysis/worktree-patches/` и удалить (`git worktree remove --force` + `git branch -D worktree-wf_*`).
- **Статус**: [ ] Не исправлено

### DEAD-02: недостижимые callback-хэндлеры recheck/priority + 3 мёртвых метода QBittorrentClient
- **Файл**: `bot/handlers/downloads.py:481` (`handle_recheck`, `t_recheck:`), `:507` (`handle_priority`, `t_prio:`); `bot/clients/qbittorrent.py:410` (`recheck`), `:418` (`set_priority_top`), `:426` (`set_priority_bottom`)
- **Проблема**: ни одна клавиатура не генерирует `t_recheck:`/`t_prio:` — продюсеров нет. Методы клиента вызываются только из этих мёртвых хэндлеров.
- **Решение**: удалить оба хэндлера и три метода клиента.
- **Статус**: [ ] Не исправлено

### DEAD-03: `Keyboards.confirm_delete_torrent` — мёртвая клавиатура с несовместимым форматом callback
- **Файл**: `bot/ui/keyboards.py:684`
- **Проблема**: не вызывается в продакшне (только тест `tests/test_qbittorrent.py:505`). Генерирует `t_delete:confirm:<hash16>` — хендлер распарсил бы `confirm:<hash16>` как short_hash и не нашёл бы торрент — сломана даже при подключении.
- **Решение**: подключить confirm-флоу перед `t_delf:` с починкой формата (продуктово правильнее), либо удалить метод + тест.
- **Статус**: [ ] Не исправлено

### DEAD-04: `AddService.grab_music_release` — ~135 строк без продакшн-вызовов
- **Файл**: `bot/services/add_service.py:694-828`
- **Проблема**: музыкальный флоу добавляет артиста только через `add_artist`. Прямого «граба» музыкального релиза в UI нет; `search_releases` с `ContentType.MUSIC` не вызывается. Единственные вызовы — тесты (`test_add_service.py:242`, `test_lidarr.py:334`, `test_r4_C4-services.py:228`).
- **Решение**: удалить вместе с тестами, либо зафиксировать комментарием как задел под фичу.
- **Статус**: [ ] Не исправлено

### DEAD-05: `Database.get_search_results` не используется; таблицы `searches`/`search_results` — write-only
- **Файл**: `bot/db.py:373`; писатель — `save_search` (`bot/db.py:335`, вызов из `bot/handlers/search.py:281`)
- **Проблема**: = DB-03/PERF-03. Каждый поиск пишет до 500 результатов в JSON, никто не читает; `/history` использует `actions`.
- **Решение**: убрать вызов `save_search` из хот-пасса (или писать без results_json); `get_search_results` удалить.
- **Статус**: [ ] Не исправлено

### DEAD-06: `ScoringService.filter_by_quality` / `get_best_result` мертвы — и настройка «🎯 Качество» ни на что не влияет
- **Файл**: `bot/services/scoring.py:275` (`get_best_result`), `:303` (`filter_by_quality`); `bot/handlers/settings.py:457`, `bot/models.py:241` (`preferred_resolution`)
- **Проблема**: оба метода вызываются только из тестов. Настройка `preferred_resolution` выставляется в UI, сохраняется в БД, отображается — но единственный потребитель никогда не вызывается. Настройка «Качество» — декоративная.
- **Риск**: Низкий (код) / Средний UX (юзер думает, что фильтр работает)
- **Решение**: подключить `preferred_resolution` к пайплайну (бонус в `calculate_score`) — код уже написан и протестирован. Либо удалить метод + пункт настроек.
- **Статус**: [ ] Не исправлено

## Низкие

### DEAD-07: `SearchService.detect_content_type` — обёртка без продакшн-вызовов
- **Файл**: `bot/services/search_service.py:65-68`
- **Проблема**: прод использует `detect_with_confidence`; обёртку вызывают лишь `tests/test_lidarr.py:284,298`.
- **Решение**: удалить, тесты перевести на `detect_with_confidence(...).content_type`.
- **Статус**: [ ] Не исправлено

### DEAD-08: неиспользуемое публичное API `NotificationService`
- **Файл**: `bot/services/notification_service.py:52` (`unsubscribe_user`), `:200` (`force_check`), `:238` (`get_stats`)
- **Проблема**: вызываются только из тестов. (`get_stats` может пригодиться для OBS-09 — сводка.)
- **Решение**: `unsubscribe_user` оставить (понадобится для DB-04/`/deluser`); `get_stats` использовать в OBS-09; `force_check` удалить.
- **Статус**: [ ] Не исправлено

### DEAD-09: `LidarrClient._parse_album` + фактически неиспользуемый `AlbumInfo`
- **Файл**: `bot/clients/lidarr.py:262-319`; `bot/models.py:178-199`, `:263` (в union `ContentInfo`)
- **Проблема**: `_parse_album` без продакшн-вызовов; `AlbumInfo` в union недостижим. Оставлен в р4 осознанно, но album-флоу так и не появился.
- **Решение**: удалить `_parse_album`, `AlbumInfo` из union и тесты; либо комментарий-обоснование.
- **Статус**: [ ] Не исправлено

### DEAD-10: шесть мёртвых констант `MENU_*` в start.py
- **Файл**: `bot/handlers/start.py:15-20`
- **Проблема**: не используются ни в файле, ни извне. Рабочие копии строк живут в 6 handler-модулях. (Связка с LOGIC-07 — централизовать.)
- **Решение**: удалить блок (или централизовать все тексты кнопок — LOGIC-07).
- **Статус**: [ ] Не исправлено

### DEAD-11: `BaseAPIClient.delete` — HTTP-метод без вызовов
- **Файл**: `bot/clients/base.py:247-254`
- **Решение**: удалить.
- **Статус**: [ ] Не исправлено

### DEAD-12: write-only поля моделей
- **Файл**: `bot/models.py:65` (`SearchResult.info_url`), `:69` (`category_names`), `:113/139/165` (`fanart_url`), `:158` (`sort_name`), `:371` (`uploaded`), `:375` (`tracker`); `bot/clients/emby.py:25-26`, `:36`
- **Проблема**: заполняются из API, никем не читаются. Для `SearchResult` — лишние байты в каждой сессии (≤500 результатов).
- **Решение**: удалить `info_url`/`category_names` у `SearchResult` (уменьшит session JSON); остальные — по желанию.
- **Статус**: [ ] Не исправлено

### DEAD-13: недостижимая ветка `filter_value == "menu"`
- **Файл**: `bot/handlers/downloads.py:622-624`
- **Решение**: удалить или комментарий «defensive».
- **Статус**: [ ] Не исправлено

### DEAD-14: избыточный кортеж исключений
- **Файл**: `bot/ui/formatters.py:970` (`except (ZoneInfoNotFoundError, Exception)`)
- **Решение**: `except Exception:`.
- **Статус**: [ ] Не исправлено

### DEAD-15: мёртвая фикстура `mock_env`
- **Файл**: `tests/conftest.py:38-41`
- **Решение**: удалить. (+ TEST-11: ещё 3 мёртвые фикстуры.)
- **Статус**: [ ] Не исправлено

### DEAD-16: два guard-теста DEPLOY-02/05 всегда скипаются (PyYAML не установлен)
- **Файл**: `tests/test_r4_C1-deploy-docs.py:112,145`
- **Проблема**: = DEP-07. Тесты compose-инвариантов никогда не выполняются.
- **Решение**: добавить pyyaml в dev-зависимости.
- **Статус**: [ ] Не исправлено

### DEAD-17: doc-rot в тестах и артефакты прошлых аудитов
- **Файл**: `tests/test_r4_C7-search-models.py:6` (docstring заявляет «monitor_type removed», хотя поле возвращено фичей #2); `analysis/*.md` (13 файлов р4 в корне analysis/)
- **Решение**: поправить docstring; отчёты р4 перенести в `analysis/r4/`.
- **Статус**: [ ] Не исправлено

### DEAD-18 (informational): последствия миграции на PageCB — кнопки старых сообщений
- **Файл**: `bot/ui/callbacks.py`, `bot/handlers/search.py:388`
- **Проблема**: легаси-парсеров `page:` не осталось — миграция чистая. Но старые сообщения содержат кнопки `page:N` без хендлера (= BUG-02). Сессии живут ≤24ч.
- **Решение**: catch-all «клавиатура устарела» (закрывается BUG-02).
- **Статус**: [ ] Не исправлено (действий не требует сверх BUG-02)

## Проверено — проблем нет

- **`bot/constants.py`** — удалён в р4, guard-тест держит; ссылок нет.
- **Неиспользуемые импорты/переменные** — ruff F401/F841/F811/F821 → чисто.
- **Осиротевшие модули** — нет: все 11 хэндлеров в `setup_routers()`, все 8 клиентов через registry, webhook config-gated.
- **Закомментированный код** — не обнаружено.
- **Классы моделей** — остальные читаются в проде; `TorrentFilter.INACTIVE` осознанно без кнопки.
- **Дубликаты тестов** — не найдены (пересечения перечислены в TEST-12).
- **Недостижимый код после return / условия-константы** — кроме DEAD-13/14 не найдено.
- **`grab_release`-обёртка** (`search.py:680`) — patch-point в тестах, оставлена сознательно.
- **Динамические вызовы** — getattr/лямбды проверены, ложных «мертвецов» нет.

**Итого**: ~400+ строк потенциально удаляемых; кодовая база после четырёх раундов в целом чистая — главные «залежи» — методы «на вырост», удерживаемые в живых только собственными тестами.
